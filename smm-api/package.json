{
  "name": "smm-rewriter-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dedicated Vercel API for the Dervin SMM Question Rewriter.",
  "scripts": {
    "dev": "npx vercel dev",
    "lint": "node --check api/rewrite.js && node --check api/feedback.js && node --check api/health.js && node --check lib/rate-limit.js",
    "test:local": "node scripts/smoke-request.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
